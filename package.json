{
  "name": "bronze-banner",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npx wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "npx wrangler d1 migrations apply bronze-banner-db --local",
    "db:migrate:remote": "npx wrangler d1 migrations apply bronze-banner-db --remote",
    "preview": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.91.0"
  }
}
