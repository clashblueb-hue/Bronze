console.error("This project now runs on Cloudflare Workers + D1.");
console.error("Use `npm run dev` for local development.");
console.error("Use `npm run deploy` to publish through Wrangler.");
process.exit(1);
